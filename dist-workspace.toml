[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The version of dist the CI scripts were generated by; `dist generate` checks
# this, so a regenerated workflow and the tool that reads it cannot drift.
dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew"]
# A GitHub repo to push Homebrew formulas to
tap = "Firaenix/homebrew-tap"
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "aarch64-unknown-linux-gnu", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in
install-path = "CARGO_HOME"
# Publish jobs to run in CI
publish-jobs = ["homebrew"]
# Which actions to run on pull requests
pr-run-mode = "plan"
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# Whether to install an updater program
install-updater = false

# rv shells out to `difft` for its structural diff and degrades to an
# in-process fallback without it. The nix package wraps difftastic onto the
# binary's PATH; `stage = ["run"]` is how the Homebrew formula says the same
# thing, so a `brew install` does not silently ship the degraded engine.
[dist.dependencies.homebrew]
difftastic = { stage = ["run"] }
